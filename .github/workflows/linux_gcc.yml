name: Ubuntu 22.04 (gcc)

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]
  workflow_call:

jobs:
  gcc:
    runs-on: ubuntu-22.04

    steps:
      - name: Checkout
        uses: actions/checkout@v3
        with:
          ref: main
      - name: call reuse clang
        uses: ./.github/workflows/linux_clang.yml
